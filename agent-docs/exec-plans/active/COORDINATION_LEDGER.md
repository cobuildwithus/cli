# Coordination Ledger (Active Only)

Use this file only for currently active coding work. Keep it minimal and current.

## Open Entries

| Agent/Session | Task | Files in Scope | Symbols (add/rename/delete) | Dependency Notes | Updated (YYYY-MM-DD) |
| --- | --- | --- | --- | --- | --- |
| codex-local-wallet-idempotency | Harden local-wallet idempotency for retry, crash-after-broadcast, and same-key concurrency scenarios. | `src/wallet/local-exec.ts`, `tests/wallet-local-exec.test.ts` | add retry-safe persisted inflight state handling in local exec flow; add focused regression coverage for same-key retry/concurrency/crash recovery | Avoid `revnet` command/test files owned by active entry; preserve caller-visible idempotency semantics for multi-step local transaction execution. | 2026-03-12 |
| codex-stake-juror-lifecycle | Integrate and audit the stake-vault juror lifecycle CLI command surface across handlers, schema metadata, docs, and tests. | `src/commands/protocol-participant-stake-premium.ts`, `src/incur/commands/stake.command.ts`, `src/cli-incur.ts`, `src/usage.ts`, `tests/protocol-participant-command.test.ts`, `tests/agent-safety-dry-run-schema.test.ts`, `README.md`, `agent-docs/cli-architecture.md`, `agent-docs/references/cli-command-and-data-flow.md`, `skills/cli/SKILL.md`, `agent-docs/exec-plans/active/2026-03-12-stake-juror-lifecycle-commands.md` | add/lock `stake opt-in-juror`, `stake request-juror-exit`, `stake finalize-juror-exit`, `stake set-juror-delegate` handler + metadata + docs/test coverage while preserving the shared protocol-plan contract | Child implementation/docs slices are complete; keep follow-up audits inside this file set only and avoid active `revnet`, `local-wallet`, `flow`, terminal, and budget-maintenance scopes. | 2026-03-12 |
| codex-flow-cli | Add the missing `flow` CLI participant commands for `sync-allocation`, `sync-allocation-for-account`, and `clear-stale-allocation` backed by shared wire planners. | `src/commands/protocol-participant-runtime.ts`, `src/commands/protocol-participant-flow.ts`, `src/incur/commands/flow.command.ts`, `src/cli-incur.ts`, `src/usage.ts`, `tests/flow-command.test.ts`, `tests/usage.test.ts`, `agent-docs/exec-plans/active/2026-03-12-flow-cli-commands.md`, `agent-docs/product-specs/cli-behavior.md` | add `flow` participant family support; add `executeFlowSyncAllocationCommand`, `executeFlowSyncAllocationForAccountCommand`, `executeFlowClearStaleAllocationCommand`; add `registerFlowCommand`; register the three missing `flow` commands; add focused flow command coverage | Reuse shared `@cobuild/wire` flow planners only; avoid active `stake`, `revnet`, and local-wallet implementation files; keep `src/cli-incur.ts`/`src/usage.ts` edits minimal and avoid stake-juror symbol overlap. | 2026-03-12 |
| codex-budget-maint-parent | Add shared CLI glue, raw-tx budget maintenance execution helpers, and coverage for budget maintenance commands backed by wire planners. | `agent-docs/exec-plans/active/2026-03-12-budget-maintenance-commands.md`, `agent-docs/exec-plans/active/COORDINATION_LEDGER.md`, `src/commands/protocol-budget-maintenance/shared.ts`, `src/commands/protocol-budget-maintenance/index.ts`, `src/incur/commands/budget.command.ts`, `tests/protocol-budget-maintenance-command.test.ts` | add budget maintenance command shared helpers + exports; add `budget` maintenance subcommand registrations and focused command coverage | Workers own the op-specific files under `src/commands/protocol-budget-maintenance/`; avoid active `revnet`, local-wallet, `flow`, and stake-juror scopes. | 2026-03-12 |
| codex2-budget-activate | Implement `budget activate` CLI command with shared hosted/local plan execution. | `src/commands/protocol-budget-maintenance/activate.ts` | add `executeBudgetActivateCommand` | Reuse parent-owned shared helpers and wire planner; do not touch budget registration/tests or other op files. | 2026-03-12 |
| codex2-budget-finalize-removed | Implement `budget finalize-removed` CLI command with shared hosted/local plan execution. | `src/commands/protocol-budget-maintenance/finalize-removed.ts` | add `executeBudgetFinalizeRemovedCommand` | Reuse parent-owned shared helpers and wire planner; do not touch budget registration/tests or other op files. | 2026-03-12 |
| codex2-budget-retry-resolution | Implement `budget retry-resolution` CLI command with shared hosted/local plan execution. | `src/commands/protocol-budget-maintenance/retry-resolution.ts` | add `executeBudgetRetryResolutionCommand` | Reuse parent-owned shared helpers and wire planner; do not touch budget registration/tests or other op files. | 2026-03-12 |
| codex2-budget-prune | Implement `budget prune` CLI command with shared hosted/local plan execution. | `src/commands/protocol-budget-maintenance/prune.ts` | add `executeBudgetPruneCommand` | Reuse parent-owned shared helpers and wire planner; do not touch budget registration/tests or other op files. | 2026-03-12 |
| codex2-budget-sync | Implement `budget sync` CLI command with shared hosted/local plan execution. | `src/commands/protocol-budget-maintenance/sync.ts` | add `executeBudgetSyncCommand` | Reuse parent-owned shared helpers and wire planner; do not touch budget registration/tests or other op files. | 2026-03-12 |
| codex-terminal-funding-parent | Register the new goal/community terminal commands, preserve the shared CLI/runtime wiring, and update command docs/schema coverage. | `src/cli-incur.ts`, `src/incur/commands/goal.command.ts`, `src/incur/commands/community.command.ts`, `src/usage.ts`, `tests/agent-safety-dry-run-schema.test.ts`, `README.md`, `skills/cli/SKILL.md`, `agent-docs/cli-architecture.md`, `agent-docs/references/cli-command-and-data-flow.md`, `agent-docs/exec-plans/active/2026-03-12-terminal-cli-commands.md` | add `community` command registration; update `goal` registration for `pay`; extend usage/docs/schema coverage for the new terminal commands | Reuse child command handlers owned by the terminal worker entries below; avoid `revnet`, stake-juror, flow, and local-wallet implementation scopes already claimed above. | 2026-03-12 |
| codex2-goal-terminal-pay | Add the CLI goal-terminal pay handler and focused regression coverage for hosted/local/dry-run execution. | `src/commands/goal-pay.ts`, `tests/goal-pay-command.test.ts` | add `executeGoalPayCommand`; add JSON-input parsing and raw-tx hosted/local pay execution coverage | Parent owns `goal.command.ts` registration/docs wiring; do not touch shared CLI runtime/docs files. | 2026-03-12 |
| codex2-community-terminal-pay | Add the CLI community-terminal pay handler and focused regression coverage for hosted/local/dry-run execution. | `src/commands/community-pay.ts`, `tests/community-pay-command.test.ts` | add `executeCommunityPayCommand`; add community pay command coverage | Parent owns `community.command.ts` registration/docs wiring; use the dedicated wire planner plus existing raw `tx` hosted/local execution path only. | 2026-03-12 |
| codex2-community-terminal-balance | Add the CLI community-terminal `addToBalanceOf` handler and focused regression coverage for hosted/local/dry-run execution. | `src/commands/community-add-to-balance.ts`, `tests/community-add-to-balance-command.test.ts` | add `executeCommunityAddToBalanceCommand`; add community-terminal add-to-balance command coverage | Parent owns `community.command.ts` registration/docs wiring; do not touch shared CLI runtime/docs files. | 2026-03-12 |
## Rules

1. Add a row before your first code edit for every coding task (single-agent and multi-agent).
2. Update your row immediately when scope or symbol-change intent changes.
3. Before deleting or renaming a symbol, check this table for dependencies.
4. Delete your row as soon as the task is complete or abandoned.
5. Leave only the header and empty table when there is no active work.
